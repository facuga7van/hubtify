<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Con WebView < 140 el contenido web arranca debajo de la barra de
             estado y esta franja muestra el fondo de la ventana: cuero
             (el token leather-dark de theme.css), no el blanco/negro del tema. -->
        <item name="android:windowBackground">#2a1d0e</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Mismo cuero (leather-dark) que el fondo de las imágenes de
             resources/splash*.png, para que el splash del sistema (API 31+)
             y el drawable no muestren franjas de otro color. -->
        <item name="windowSplashScreenBackground">#2a1d0e</item>
        <item name="android:windowBackground">#2a1d0e</item>
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>